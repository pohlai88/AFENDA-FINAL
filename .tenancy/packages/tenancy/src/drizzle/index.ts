/**
 * @domain tenancy
 * @layer drizzle
 * @responsibility Tenancy database schema (re-export from shared)
 * @note Schema definitions live in lib/server/db/schema for now.
 *       This barrel re-exports tenancy-related tables.
 */

// TODO: Migrate schema from lib/server/db/schema/index.ts
// For now, we re-export from the shared schema location

export {}
